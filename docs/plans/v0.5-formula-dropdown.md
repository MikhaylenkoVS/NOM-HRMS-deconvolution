# План: выпадающий список формул в таблице результатов

> **Создан:** 2026-07-11 | **Статус:** 🔴 в работе | **Дедлайн:** 2026-07-14

## Суть

В колонке «Формула» таблицы результатов — Combobox с альтернативными кандидатами.
Автоматически подбирается лучший кандидат по критериям NOM-приоритизации.
Пользователь может вручную изменить формулу на любую из предложенных.
При смене формулы перестраиваются Van Krevelen и вкладка «Структуры».

## Шаги

| # | Задача | Статус |
|---|--------|--------|
| 1 | `assign_formulas_simple`: сохранять всех кандидатов в колонку `candidates` | [ ] |
| 2 | `build_result_table`: пробрасывать `candidates` в итоговую таблицу | [ ] |
| 3 | `_fill_result_table`: combobox overlay по двойному клику на ячейку «Формула» | [ ] |
| 4 | `_on_candidate_selected`: обновление result_df + перестроение VK/Structures | [ ] |
| 5 | Unit-тест: проверка структуры `candidates` | [ ] |
| 6 | Интеграционный тест: candidates в итоговой таблице | [ ] |

## Контекст (из сессии на старом ПК)

### Текущее состояние dev
- `9d56293` — N-штраф в NOM-приоритизации (N>3 + O/N<0.5)
- `9d56293` — теоретическая масса для поиска серий (вместо экспериментальной)
- `4a1e4b9` — выбор метода денойза в GUI (force/intensity/quantile)

### Где лежит код
- `src/core/spectrum_ops.py` — `assign_formulas_simple()` (строка ~629), `build_result_table()` (~1157)
- `src/app.py` — `_fill_result_table()` (~962), Van Krevelen (~1051), Structures tab

### Алгоритм candidates
```python
# В assign_formulas_simple, после выбора лучшей формулы:
cands = []
for li in np.where(mask)[0]:
    cands.append({"formula": str(cand_formulas[li]), "ppm": float(abs_ppm[li])})
cands.sort(key=lambda x: x["ppm"])
table.at[idx, "candidates"] = cands
```

### Combobox overlay
- Двойной клик по колонке #2 → создать Combobox поверх ячейки
- При выборе: обновить `result_df.at[row_idx, "brutto"]` → перестроить таблицу и VK
- Combobox.destroy() при FocusOut

### Стандартные значения денойза (из литературы)
- Force (S/N multiplier): 1.5 (default), 3 для зашумлённых данных
- Absolute intensity: 100 (default), зависит от прибора
- Quantile: 0.01 (default), 0.05 для агрессивной фильтрации
